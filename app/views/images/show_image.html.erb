<div class="container">
  <div class="img_block">
        <h3><%= @show_image.description %></h3> <br>
        <%= image_tag @show_image.img.url %>
        <p>Tags: <%= raw(@show_image.tag_list.map { |t| link_to t, tag_path(t) }.join(', ')) %></p>

  </div>
</div>